#ifndef NUMTOTEXT_HPP
#define NUMTOTEXT_HPP

#include <string>
#include <clocale>
#include <cmath>
#include <vector>

std::string textize3(int n, bool thousands);
std::string textize(std::string n);

#endif  // NUMTOTEXT_HPP
